import { Typography } from "@mui/material";

export function Heading() {
  return (
    <Typography
      variant="h1"
      sx={{
        fontSize: "2rem",
        marginTop: "2rem",
        marginBottom: "2rem",
      }}
    >
      GM's Notebook Local Server
    </Typography>
  );
}
